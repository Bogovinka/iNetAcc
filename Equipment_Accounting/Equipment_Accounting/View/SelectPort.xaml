<Window x:Class="Equipment_Accounting.View.SelectPort"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Equipment_Accounting.View"
        mc:Ignorable="d"
        Title="SelectPort" Height="450" Width="800" WindowStartupLocation="CenterScreen" MinHeight="450" MinWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="319*"/>
            <RowDefinition Height="46*"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" HorizontalScrollBarVisibility="Auto">
            <TreeView x:Name="treeView" FontSize="14" FontWeight="Bold" Foreground="Black">
            </TreeView>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBox x:Name="searchText" MaxWidth="200" KeyDown="searchText_KeyDown"  Margin="10,0,0,0" VerticalAlignment="Center" MinWidth="200" FontSize="18" Cursor="IBeam"></TextBox>
            <Button x:Name="search" Margin="10,0,0,0" Click="search_Click" FontSize="18" Cursor="Hand">Выделить</Button>
        </StackPanel>
        <Button Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,10,0" Click="Button_Click">Выбрать</Button>
    </Grid>
</Window>
